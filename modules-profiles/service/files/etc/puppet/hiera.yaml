---
:backends:
  - yaml
:yaml:
  :datadir: /etc/puppet/hiera
:hierarchy:
  - "common/lists"
  - "node/%{::fqdn}"
  - "architecture/%{::architecture}"
  - "virtual/%{::virtual}"
  - "makeconf/use_apache_%{service::portage::make_conf::use_apache::add}"
  - "common/single"
:merge_behavior: deeper
