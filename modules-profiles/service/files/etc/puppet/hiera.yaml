---
:backends:
  - yaml
:yaml:
  :datadir: /etc/puppet/hiera
:hierarchy:
  - "common/lists"
  - "node/%{::fqdn}"
  - "architecture/%{::architecture}"
  - "virtual/%{::virtual}"
  - "useflags/apache_%{service::portage::use_apache}"
  - "common/single"
:merge_behavior: deeper
